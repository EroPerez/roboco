# 🤖 roboco

**roboco** es un proyecto automatizado desarrollado en Python. Utiliza Selenium para tareas de automatización web y captura de pantallas.

## 📁 Estructura del Proyecto

```bash
roboco/
├── geckodriver.log   # Registro del controlador de Firefox
├── main.py           # Punto de entrada principal
├── requirements.txt  # Dependencias del proyecto
├── screenie.png     # Captura de pantalla generada
├── venv/            # Entorno virtual de Python
```

## 🚀 Instalación

1. Clona el repositorio:
   ```bash
   git clone https://tu-repo.com/roboco.git
   cd roboco
   ```

2. Activa el entorno virtual:
   ```bash
   source venv/bin/activate  # Linux/Mac
   # o
   venv\Scripts\activate     # Windows
   ```

3. Instala las dependencias:
   ```bash
   pip install -r requirements.txt
   ```

## ▶️ Uso

Ejecuta el proyecto con:

```bash
python main.py
```

## 📸 Salida

El proyecto genera una captura de pantalla en `screenie.png` al ejecutarse.

---

Desarrollado con ❤️ y Python 🐍
